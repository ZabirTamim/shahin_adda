import 'package:flutter/material.dart';
import 'package:get/get.dart';

class BakiPage extends StatelessWidget {
  const BakiPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        color: Colors.green,
        child: Text("BakiPage"),
      ),
    );
  }
}
